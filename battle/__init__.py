"""SentAInce battle-test library (M0+) — the additive, importable building blocks for the live,
containerized homeostasis demonstration.

Everything here is ADDITIVE and imports the locked organs (``sentaince.organism.*``) READ-ONLY.
Nothing in this package modifies the locked experiments or the 99-test deterministic lock under
``tests/``. When driven by a real LLM head this is a **labeled, non-deterministic DEMONSTRATION**,
never part of the C1–C7 evidence lock (see ``docs/CLAIM_BOUNDARY.md`` and ``battle/README.md``).

M0 scope (this commit): the runner skeleton on a symbolic body. The composed somatic gate
(``somatic_gate.composed_gate``) reproduces ``experiments/exp7_crucible.py``'s grand-ambush outcome
exactly when driven by the deterministic ``ScriptedProposer`` — proving the harness is wired before
any container or daemon is involved.
"""
