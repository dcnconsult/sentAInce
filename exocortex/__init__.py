"""Exocortex — a Claude Code hook control plane (Track A.2, a labeled demonstration).

A prosthetic cognitive layer *outside* the model's weights that manipulates the soft layer
(injected context) and the hard layer (tool permissions) to (a) **veto** structurally-lethal /
destructive actions and (b) **inject interoceptive gauges** that break Compute Agnosia and
Strategy-Lock. It reuses the LOCKED organs read-only:

  * hard layer  — ``battle.somatic_gate.gate`` (C1 interlock + C4 antibody + C6 oracle) via a
                  ``PreToolUse`` deny; the energy-independent brake, now on a real tool call.
  * soft layer  — an interoceptive block (energy tier + strategy-lock) via ``UserPromptSubmit``
                  ``additionalContext``; the v1.02 metabolism + v1.06/v1.07 epistemic laws, applied.

This is NOT part of the C1–C7 evidence lock and imports the locked code read-only. See
``docs`` / the plan ``snazzy-roaming-brook.md``.
"""
