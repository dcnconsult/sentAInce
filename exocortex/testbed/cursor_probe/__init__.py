"""Cursor hook PROBE — the P0 measurement harness for the model-independent IDE arc.

A read-only instrument (imports nothing from the locked organism; never deposits τ). It captures real Cursor
hook payloads and tests every candidate injection channel so the design's P0 questions are answered by
measurement, not assumption (ADR-002 gauge-first). See README.md and ENHANCEMENTS.md §H.
"""
