"""Declarative substrate (NEXT_PHASE_PLAN §6 / Wave-2 Ticket 1) — the living Markdown wiki.

The basal-ganglia colony (`exocortex.colony`) is procedural memory (*how*). This package is the
hippocampus/neocortex: declarative memory (*what things are*) — a Markdown Zettelkasten infected
with the FreqOS substrate so it self-curates by consequence instead of decaying into dead RAG.

Entry point: `node.WikiGraph` — the dual-lane data structure. See `node` for the design contract.
"""
